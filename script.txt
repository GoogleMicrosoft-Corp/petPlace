create table USUARIOS (
  USUARIOS_ID INT PRIMARY KEY IDENTITY(1,1),
  NOME VARCHAR(100),
  USR_LOGIN VARCHAR(100),
  USR_HASH VARCHAR(100)
)
GO

--INSERT INTO USUARIOS (NOME, USR_LOGIN, USR_HASH) 
--VALUES ('LEEANDRO VARGAS','leandro','123') 

--select * from USUARIOS

exec sp_rename 'USUARIOS.[USR_LOGIN]', 'EMAIL', 'column'
GO

---------

ALTER TABLE usuarios
ADD CPF VARCHAR (14)
go

ALTER TABLE usuarios
ADD Telefone VARCHAR (15)
go


ALTER TABLE usuarios
ADD Endereco VARCHAR (200)
go

ALTER TABLE usuarios
ADD Descricao VARCHAR (MAX)
go

---------
create table PETS(
  PET_ID INT PRIMARY KEY IDENTITY(1,1),
  NOME_PET VARCHAR(200),
  TIPO_PET INT,
  SEXO CHAR(1),
  IDADE INT,
  DESCRICAO VARCHAR(1000),
  DOADOR_ID INT
)
GO
--------
CREATE TABLE IMAGEM_USUARIO(
  IMAGEM_USUARIO_ID INT PRIMARY KEY IDENTITY(1,1),
  USUARIOS_ID INT,  
  DADOS VARCHAR(max),
  TIPO VARCHAR(20)
)
GO
-----
CREATE TABLE IMAGEM_PET(
  IMAGEM_PET_ID INT PRIMARY KEY IDENTITY(1,1),
  PET_ID INT,  
  DADOS VARCHAR(max),
  TIPO VARCHAR(20),
  ATUAL VARCHAR(1)
)
GO
-----
/*
  --deprecado
  create table POST(
    POST_ID INT PRIMARY KEY IDENTITY(1,1),
    PET_ID INT,
    DESCRICAO VARCHAR(1000),
    ADOTADO CHAR(1)
  )
*/

create table POST(
  POST_ID INT PRIMARY KEY IDENTITY(1,1),
  PET_ID INT,
  USUARIO_ID INT,
  DESCRICAO VARCHAR(1000),
  STATUS_PUBLICACAO CHAR(100),
  IMAGEM varchar(max),
  TIPO_IMAGEM varchar(10),
  DATA DATETIME,
)
GO
-----
create table DOADOR(
  DOADOR_ID INT,
  PET_ID INT,
  POST_ID INT,
  ADOTADO CHAR(1)
)

---


create table PETS_PERDIDOS(
  PET_ID INT PRIMARY KEY IDENTITY(1,1),
  NOME_PET VARCHAR(200),
  TIPO_PET INT,
  SEXO CHAR(1),
  IDADE INT,
  DESCRICAO VARCHAR(1000),
  DOADOR_ID INT
)
GO
CREATE TABLE IMAGEM_PET_PERDIDO(
  IMAGEM_PET_ID INT PRIMARY KEY IDENTITY(1,1),
  PET_ID INT,  
  DADOS VARCHAR(max),
  TIPO VARCHAR(20),
  ATUAL VARCHAR(1)
)
GO

create table CONFIGURACOES(
  CONFIGURACOES_ID INT PRIMARY KEY IDENTITY(1,1),
  COR_INICIAL VARCHAR(10),	
  COR_FINAL VARCHAR(10),
  USUARIO_ID INT
)
go
